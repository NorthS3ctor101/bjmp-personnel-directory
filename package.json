{
  "name": "prmis-search-portal",
  "version": "2.1.0",
  "description": "BJMP PRMIS Personnel Directory – secure search portal",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build:css": "npx tailwindcss -i ./tailwind.input.css -o ./public/vendor/tailwind.css --minify",
    "start": "cross-env NODE_ENV=production node server.js",
    "dev": "cross-env NODE_ENV=development node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^8.0.0"
  },
  "devDependencies": {
    "lucide": "^0.383.0",
    "tailwindcss": "^4.3.1"
  }
}
